<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Document</title>
</head>
<body>
    <table>
        <th class="view-table">Username</th>
        <th class="view-table">Rating</th>
        <th class="view-table">Total Credit</th>
        <th class="view-table">Credit Allocated</th>
        <% customerCreditCheck.forEach(customer => {%>
            <tr>
                <td class="view-table"><%= customer.customer_name %></td>
                <td class="view-table"><%= customer.rating %></td>
                <td class="view-table"><%= customer.current_credit %></td>
                <td class="view-table"><%= customer.credit_already_allocated %></td>
            </tr>
        <% }) %>
    </table>
    <div class="row">
        <a href="/views" class="long button">Back</a>
    </div>
</body>
</html>