<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Document</title>
</head>
<body>
    <table>
        <th class="view-item">Store ID</th>
        <th class="view-item">Store Name</th>
        <th class="view-item">Store Manager</th>
        <th class="view-item">Store Revenue</th>
        <th class="view-item">Incoming Revenue</th>
        <th class="view-item">Incoming Orders</th>
        <% storeSalesOverview.forEach(store => {%>
            <tr>
                <td class="view-item"><%= store.store_id %></td>
                <td class="view-item"><%= store.sname %></td>
                <td class="view-item"><%= store.manager %></td>
                <td class="view-item"><%= store.revenue %></td>
                <td class="view-item"><%= store.incoming_revenue %></td>
                <td class="view-item"><%= store.incoming_orders %></td>
            </tr>
        <% }) %>
    </table>
    <div class="row">
        <a href="/views" class="long button">Back</a>
    </div>
</body>
</html>