<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Document</title>
</head>
<body>
    <table>
        <th class="view-table">Order ID</th>
        <th class="view-table">Order Cost</th>
        <th class="view-table">Product Count</th>
        <th class="view-table">Payload</th>
        <th class="view-table">Contents</th>
        <% ordersInProgress.forEach(order => {%>
            <tr>
                <td class="view-table"><%= order.orderID %></td>
                <td class="view-table"><%= order.cost %></td>
                <td class="view-table"><%= order.num_products %></td>
                <td class="view-table"><%= order.payload %></td>
                <td class="view-table"><%= order.contents %></td>
            </tr>
        <% }) %>
    </table>
    <div class="row">
        <a href="/views" class="long button">Back</a>
    </div>
</body>
</html>